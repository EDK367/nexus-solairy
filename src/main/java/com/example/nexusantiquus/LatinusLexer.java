package com.example.nexusantiquus;

import org.antlr.v4.runtime.CharStream;
import org.nexus.nexussolairy.PigLatinLexer;

public class LatinusLexer extends PigLatinLexer {
    public LatinusLexer(CharStream input) {
        super(input);
    }
}
